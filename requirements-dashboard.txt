# Host Optimizer app + models. Install into the project venv:
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements-dashboard.txt
streamlit>=1.40
plotly>=5.20
pandas>=2.0
numpy>=1.24
scikit-learn>=1.4     # revenue/price models (python/06_model.py)
joblib>=1.3           # load the saved models in the app
statsmodels>=0.14     # lowess trendlines
openpyxl>=3.1         # read listings.xlsx in the cleaning step
